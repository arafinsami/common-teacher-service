package com.eteacher.service.resource;

import org.springframework.web.bind.annotation.CrossOrigin;

@CrossOrigin
public class BaseResource {

    // Logger logger = LoggerFactory.getLogger(BaseResource.class);

}
